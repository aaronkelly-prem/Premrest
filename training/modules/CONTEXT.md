# Modules — per-module output

This folder holds the built modules. One subfolder per module, created when that module is scripted:

```
modules/<module>/
  script.md
  test-bank.md
  practical-checklist.md
  shot-list.md
```

The locked module map (streams, tiers, prerequisites, build order, status) lives in `../project/modules-map.md`. The templates and the development loop live in `../project/module-production-kit.md`.

Status: nothing built yet.
